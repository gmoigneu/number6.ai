---
const steps = [
	{
		num: "01",
		title: "We respond (fast).",
		description:
			"You'll hear back from a real person, not a bot, not an autoresponder. We typically reply within a few hours during business hours, and always within one business day.",
	},
	{
		num: "02",
		title: "We have a real conversation.",
		description:
			"We'll set up a quick call to understand your situation and your goals. We'll tell you if AI is the right move, and we'll tell you if it isn't.",
	},
	{
		num: "03",
		title: "We give you a clear next step.",
		description:
			"You'll leave with a clear next move: a proposal, a recommendation, or a straight 'this isn't the right fit.' If we can't help, we'll point you to someone who can.",
	},
];
---

<section class="flex flex-col items-center gap-12 bg-background px-16 py-[100px] w-full">
  <h2 class="font-heading text-[40px] font-bold text-foreground text-center leading-[1.1] tracking-[-1.5px]">
    Here's what happens next.
  </h2>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-8 w-full">
    {steps.map((step) => (
      <div class="flex flex-col gap-4 border-t-4 border-accent pt-8">
        <span class="font-heading text-[48px] font-bold text-accent tracking-[-2px]">{step.num}</span>
        <h3 class="font-heading text-xl font-bold text-foreground tracking-[-0.5px]">{step.title}</h3>
        <p class="font-body text-sm text-muted-foreground leading-[1.7]">{step.description}</p>
      </div>
    ))}
  </div>
</section>
