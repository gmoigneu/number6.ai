---
const stats = [
	{ value: "5-10 hrs", label: "SAVED PER EMPLOYEE\nPER WEEK" },
	{ value: "30-90", label: "DAYS FROM CALL TO\nWORKING SOLUTION" },
	{ value: "8-25", label: "TEAM MEMBERS TRAINED\nPER ENGAGEMENT" },
	{
		value: "80%+",
		label: "CLIENTS EXPAND TO\nADDITIONAL PROJECTS",
		highlight: true,
	},
];
---

<section class="flex flex-col gap-16 px-4 md:px-8 lg:px-16 py-[100px] w-full">
  <!-- Header -->
  <div class="flex flex-col items-center gap-4 w-full">
    <span class="font-heading text-[11px] font-semibold tracking-[2px] text-accent">RESULTS</span>
    <h2 class="font-heading text-[56px] font-bold text-foreground text-center tracking-[-2px]">Real work. Real numbers.</h2>
  </div>

  <!-- Quote block -->
  <div class="flex flex-col items-center gap-6 border-2 border-foreground px-4 md:px-8 lg:px-16 py-12 w-full">
    <span class="font-heading text-[80px] font-bold text-accent leading-[0.5]">&ldquo;</span>
    <p class="font-body text-lg italic text-foreground text-center leading-[1.7] max-w-[800px]">
      Most businesses we assess are sitting on 3-5 AI opportunities they haven't spotted yet. The average number6 engagement starts saving people real time within 90 days.
    </p>
  </div>

  <!-- Stats row -->
  <div class="grid grid-cols-4 gap-0 w-full">
    {stats.map((stat) => (
      <div class="flex flex-col items-center gap-3 border border-foreground p-8">
        <span class={`font-heading text-4xl font-bold tracking-[-1px] ${stat.highlight ? "text-accent" : "text-foreground"}`}>
          {stat.value}
        </span>
        <span class="font-heading text-[11px] font-semibold tracking-[1px] text-muted-foreground text-center leading-normal whitespace-pre-line">
          {stat.label}
        </span>
      </div>
    ))}
  </div>
</section>
